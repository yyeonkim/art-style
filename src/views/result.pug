extends base
include mixins/artwork

block content 
    main.result__main
        div.target
            h1 검색한 이미지
            img.target__img(src=imgSrc)
        div.list
            each artwork in artworks
                +artwork(artwork)

block scripts 
    script(src="/js/result.js")